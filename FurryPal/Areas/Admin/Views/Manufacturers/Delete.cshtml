@model FurryPal.Web.Areas.Admin.ViewModels.Manufacturers.EditDeleteManufacturerViewModel

@{
    ViewData["Title"] = "Delete Manufacturer";
}

<h2 class="text-center">@ViewData["Title"]</h2>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-6">
            <section>
                <form asp-area="Admin" asp-controller="Manufacturers" asp-action="DeleteManufacturer" method="post">
                    <div class="form-group">
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" disabled value="@Model.Name"/>
                    </div>
                    <div class="form-group">
                        <label asp-for="Email"></label>
                        <input asp-for="Email" class="form-control" disabled value="@Model.Email"/>
                    </div>
                    <div class="form-group">
                        <label asp-for="PhoneNumber"></label>
                        <input asp-for="PhoneNumber" class="form-control" disabled value="@Model.PhoneNumber"/>
                    </div>
                    <div class="form-group text-center">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </div>
                </form>
                <div class="form-group text-center">
                    <button onclick="window.history.back()" class="btn btn-primary">Cancel</button>
                </div>
            </section>
        </div>
    </div>
</div>