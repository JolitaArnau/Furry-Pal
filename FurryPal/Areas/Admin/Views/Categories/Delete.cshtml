@model FurryPal.Web.Areas.Admin.ViewModels.Categories.EditDeleteViewModel

@{
    ViewData["Title"] = "Edit Category";
}

<h2 class="text-center">@ViewData["Title"]</h2>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-6">
            <section>
                <form asp-area="Admin" asp-controller="Categories" asp-action="DeleteCategory" method="post">
                    <div class="form-group">
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" disabled value="@Model.Name"/>
                    </div>
                    <div class="form-group">
                        <label asp-for="Description"></label>
                        <input asp-for="Description" class="form-control" disabled value="@Model.Description"/>
                    </div>
                    <div class="form-group text-center">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </div>
                </form>
                <div class="form-group text-center">
                    <button onclick="window.history.back()" class="btn btn-primary">Cancel</button>
                </div>
            </section>
        </div>
    </div>
</div>